{
  "name": "udemy-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Udemy instructor dashboard — Express API + React (Vite) frontend",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/index.js",
    "scrape": "npm run scrape:enrollment --prefix server"
  }
}
